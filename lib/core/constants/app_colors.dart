import 'package:flutter/material.dart';

class AppColors {
  // 🔥 Primary Brand Color (AptCoder Orange)
  static const Color primary = Color(0xFFE85D04);

  // Slight darker orange (buttons, hover, emphasis)
  static const Color primaryDark = Color(0xFFD9480F);

  // Light orange (backgrounds, cards)
  static const Color primaryLight = Color(0xFFFFE5D9);

  // 🧾 Background Colors
  static const Color scaffoldBackground = Color(0xFFFFFFFF);
  static const Color cardBackground = Color(0xFFF9F9F9);

  // 🖋 Text Colors
  static const Color textPrimary = Color(0xFF1C1C1C);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textHint = Color(0xFF9CA3AF);

  // 🧱 Border & Divider
  static const Color border = Color(0xFFE5E7EB);
  static const Color divider = Color(0xFFEEEEEE);

  // ✅ Success / ❌ Error / ⚠ Warning
  static const Color success = Color(0xFF16A34A);
  static const Color error = Color(0xFFDC2626);
  static const Color warning = Color(0xFFF59E0B);

  // 🔘 Buttons
  static const Color buttonPrimary = primary;
  static const Color buttonText = Colors.white;

  // 🧑‍🎓 Student / Admin Badges
  static const Color adminBadge = Color(0xFF1D4ED8);
  static const Color studentBadge = Color(0xFF059669);
}
